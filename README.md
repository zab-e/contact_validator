📇 Single Contact Validator (C)

This is a simple C program that collects and validates one contact using a struct.

🔧 Features

Takes a contact’s name

Takes a country code (ke or tz)

Takes a phone number

Validates input (empty name, invalid country code)

Prints a formatted summary with the correct country prefix

🧠 Concepts Used

struct

String input with fgets

String comparison using strcmp

Length checking with strlen

Conditional logic (if / else)
